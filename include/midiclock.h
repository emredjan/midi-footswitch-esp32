#include <Arduino.h>
#include <ShiftRegister74HC595.h>
#include <MIDI.h>

// extern const byte MIDI_RT_START;
// extern const byte MIDI_RT_STOP;
// extern const byte MIDI_RT_CLOCK;

// extern unsigned long bpm;
// extern unsigned long prevBpm;
// extern unsigned long usPerTick;
// extern unsigned long prevTime;

// extern const unsigned long MIN_BPM;
// extern const unsigned long MAX_BPM;

// extern unsigned long currentTimer[5];
// extern unsigned long lastTap;

// extern unsigned long usPerTap;
// extern byte tapCounter;

// extern bool midiClockState;

// extern unsigned long prevBlink;
// extern bool ledState;

// void tapTempo(ShiftRegister74HC595<3> sr, midi::MidiInterface<midi::SerialMIDI<HardwareSerial>> MIDI);
// void handleMidiClock(midi::MidiInterface<midi::SerialMIDI<HardwareSerial>> MIDI);
// void handleTempoLed(ShiftRegister74HC595<3> sr);
